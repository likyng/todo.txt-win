<Page
    x:Class="todotxt.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:todotxt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="wideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="641" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="narrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="inputPanel.Orientation" Value="Vertical"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="inputPanel" Orientation="Horizontal" Margin="0, 20, 0, 20" Grid.Row="0">
            <TextBox x:Name="inputBox" Width="Auto" Margin="5, 0, 5, 0"/>
            <Button x:Name="applyButton" Content="Apply!" Click="applyButton_Click"/>
            <Button x:Name="fileOpen" Content="Open" Click="fileOpen_Click" HorizontalAlignment="Right"/>
            <Button x:Name="settingsButton" Content="Settings" Click="settingsButton_Click">
                <Button.Flyout>
                    <Flyout>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Settings"/>
                            <StackPanel Orientation="Horizontal" Margin="3">
                                <TextBlock Text="todo.txt File: " VerticalAlignment="Center"/>
                                <Button Content="Choose File" Margin="2"/>                                
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="3">
                                <TextBlock Text="done.txt File: " VerticalAlignment="Center"/>
                                <Button Content="Choose File" Margin="2"/>
                            </StackPanel>
                            <CheckBox Content="Auto Archive"/>
                            <CheckBox Content="Auto add Date"/>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </StackPanel>
            <ListBox x:Name="todoList" Margin="5, 5, 5, 5" Grid.Row="1"/>
            
    </Grid>
</Page>
